Prerequisites:

Visual Studio 2017

Instructions:

Open the solution file (Win/Swig.sln) is Visual Studio IDE, select
'Release' configuration and then build the solution. Upon successful
compilation, a Swig.exe executable should be generated at Win/Release
folder. The executable file should be then copied to the project root folder.
